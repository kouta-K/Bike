<div class="login">
    <h1>Login</h1>
    <% if @errors %>
            <ul class="login-error">
            <% @errors.each do |error| %>
                <li><%= error %></li>
            <% end %>
            </ul>
        <% end %>
    <%= form_with(url: login_path,local: true, scope: :session, class: "login-form") do |f| %>
        <%= f.label :email, "Email" %>
        <%= f.email_field :email %>
        
        <%= f.label :password, "Password" %>
        <%= f.password_field :password %>
        <div class="login-btn">
            <%= f.submit "Login", class: "btn" %>
        </div>
    <% end %>
    <p>アカウントをお持ちでない方は<%= link_to "こちら", new_user_path %></p>
    <p>パスワードを忘れた場合は<%= link_to "こちら", new_password_reset_path() %></p>
</div>
